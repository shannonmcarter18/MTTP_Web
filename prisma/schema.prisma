// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
//Carson's Branch inspo.
//Important prisma command 
//npx prisma db push --> applies current schema to database. Will not generate a migration file (contributors cant apply/validation the migration)
// npx prisma migrate dev 
//npx prisma generate 
